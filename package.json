{
  "name": "leo-ferraz-brand",
  "private": true,
  "type": "module",
  "scripts": {
    "tokens:validate": "node scripts/validate-tokens.mjs",
    "tokens:build": "node scripts/build-tokens.mjs",
    "dev": "npm run tokens:validate && npm run tokens:build && astro dev",
    "build": "npm run tokens:validate && npm run tokens:build && astro build",
    "preview": "astro preview"
  },
  "engines": {
    "node": ">=22.19.0 <23"
  },
  "devDependencies": {
    "astro": "7.2.2"
  },
  "dependencies": {
    "@fontsource-variable/ibm-plex-sans": "5.3.0",
    "@fontsource/ibm-plex-mono": "5.3.0"
  }
}
